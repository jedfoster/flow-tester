{
  "name": "flow-tester",
  "version": "1.1.8",
  "description": "",
  "main": "index.js",
  "engines": {
    "node": ">=14.15.0",
    "npm": ">=6.14.8",
    "yarn": "^1.22.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "release": "standard-version -a"
  },
  "author": "",
  "license": "ISC",
  "standard-version": {
    "scripts": {
      "posttag": "git push --follow-tags --no-verify"
    },
    "releaseCommitMessageFormat": "[ci skip] chore(release): {{currentTag}}"
  },
  "devDependencies": {
    "standard-version": "^9.3.1"
  }
}
